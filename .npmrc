# Vercel runs installs with a frozen lockfile in CI by default.
# This repo uses pnpm patchedDependencies; lockfile hashing can differ across OS line-endings.
# Disable frozen lockfile to keep demo deployments unblocked.
frozen-lockfile=false
